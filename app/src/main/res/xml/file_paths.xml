<?xml version="1.0" encoding="utf-8"?>
<paths
    xmlns:android = "http://schemas.android.com/apk/res/android">
    <external-path name = "hidden" path = "/Pictures/Porong"/>

    <!-- external-path represents files in the root of the external storage area
    the root path is the same as the value returned by Environment(=getExternalStorage()).
    The file path is return value of getExternalStorage() + /porong_image-->

</paths>